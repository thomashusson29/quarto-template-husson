name: Render formats

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  render:
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: quarto-dev/quarto-actions/setup@8a96df13519ee81fd526f2dfca5962811136661b # v2.2.0
        with:
          version: 1.8.26
          tinytex: true

      - uses: r-lib/actions/setup-r@d3c5be51b12e724e68f33216ca3c148b66d5f0b6 # v2

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - uses: r-lib/actions/setup-r-dependencies@d3c5be51b12e724e68f33216ca3c148b66d5f0b6 # v2
        with:
          extra-packages: |
            any::ggplot2
            any::knitr
            any::magick
            any::reticulate

      - name: Install PDF inspection tools
        run: |
          sudo apt-get update
          sudo apt-get install --yes poppler-utils

      - name: Render the neutral template with the project command
        run: quarto render

      - name: Render the R/Python example
        run: quarto render examples/r-python-reticulate.qmd

      - name: Check generated artifacts
        shell: bash
        run: |
          test -s output/template.html
          test -s output/template.pdf
          test -s examples/r-python-reticulate.html
          grep -q "Predicted" examples/r-python-reticulate.html
          grep -q "auto-dark-system.js" output/template.html
          grep -q "auto-dark-renderings.js" output/template.html
          pdfinfo output/template.pdf | grep -q '^Pages:'
